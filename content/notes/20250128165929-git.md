---
title: "git"
date: 2025-01-28T00:00:00+01:00
lastmod: 2025-03-06T00:00:00+01:00
tags: ["tools"]
draft: false
---

## <span class="org-todo todo TODO">TODO</span> Add explanations to all commands {#add-explanations-to-all-commands}


## Help {#help}

`git help comand`
`man git-comand`
`git comand --help`


## Start {#start}

`git init`
`git clone url`


## Commiting {#commiting}

`git commit`
`git commit -m "message"`
`git commit -a`
`git commit --amend`


### Going back to a previous state {#going-back-to-a-previous-state}

`git revert idCommit`

`git reset HEAD file file`
`git reset --hard`
`git reset --soft`


### [Conventional Commits]({{< relref "20250127105834-conventional_commits.md" >}}) {#conventional-commits--20250127105834-conventional-commits-dot-md}


## Add and remove files {#add-and-remove-files}

`git add file`
`git add -i`

`git rm file`
`git rm --cached file`
`git mv file newfile`

`git clear`


## Branching {#branching}

`git branch branchname`
`git branch -d branchname`
`git checkout branchname`
`git checkout file`
`git checkout -- file`


### rebase {#rebase}

`git rebase branchname`

`git rebase -i commit` (fav command)


### merge {#merge}

`git merge branchname`


## Handle remotes {#handle-remotes}

`git pull`
`git remote -v`
`git add remote-name url`
`git fetch remote-name`
`git push remote-name branch-name`
`git remote show remote-name`
`git remote rm remote-name`
`git remote rename name newname`


## Stash {#stash}

`git stash`
`git stash pop`
`git stash clear`


## Tags {#tags}

`git tag`
`git tag -a tagname -m "tag anotation"`
`git push origin tagname`
`git push origin --tags`


## Status and logs {#status-and-logs}

`git status`

`git log`
`git log -n number`
`git log -p diff id1 id2`
`git log --graph`
